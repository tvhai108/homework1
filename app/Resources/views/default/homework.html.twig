{# app/Resources/views/lucky/number.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>add some text: {{ form_start(form) }} </h1>

        {{ form_errors(form) }}
        <div>
            {{ form_label(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}
        </div>

        {{ form_label(form, 'Task Description') }}

    <h1>{{ form_end(form) }}<h1>
{% endblock %}
